<UserControl x:Class="UltraTask.Controls.InlineTextEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <!-- Modo visualização: TextBlock clicável -->
        <TextBlock x:Name="ViewBlock"
                   Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   FontWeight="{Binding FontWeight, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   VerticalAlignment="{Binding VerticalAlignment, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Cursor="IBeam"
                   Visibility="Visible" />

        <!-- Modo edição: TextBox -->
        <TextBox x:Name="EditBox"
                 Text="{Binding Text, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                 FontSize="{Binding FontSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
                 FontWeight="{Binding FontWeight, RelativeSource={RelativeSource AncestorType=UserControl}}"
                 Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=UserControl}}"
                 Background="Transparent"
                 BorderThickness="0,0,0,1"
                 BorderBrush="{DynamicResource Accent}"
                 CaretBrush="{DynamicResource TextPrimary}"
                 Padding="0"
                 VerticalAlignment="Center"
                 KeyDown="OnEditKeyDown"
                 LostFocus="OnEditLostFocus"
                 Visibility="Collapsed" />
    </Grid>
</UserControl>
